<?xml version="1.0"?>
<ruleset>
    <config name="installed_paths" value="../../slevomat/coding-standard"/>
    <!-- Include full PSR12 Coding Standard -->
    <rule ref="PSR12"/>

    <!-- Add rule to sniff for trailing commas for arrays -->
    <rule ref="SlevomatCodingStandard.Arrays.TrailingArrayComma"/>
</ruleset>
